# Kopieren nach .env.local (lokal) bzw. als Umgebungsvariablen bei Hostinger hinterlegen.
# Werte werden erst eingetragen, wenn der jeweilige Baustein angebunden wird.

# --- Datenbank (Strato, MySQL/MariaDB) ---
DB_HOST=
DB_PORT=3306
DB_NAME=
DB_USER=
DB_PASSWORD=

# --- ChurchTools ---
CHURCHTOOLS_BASE_URL=
CHURCHTOOLS_TOKEN=
